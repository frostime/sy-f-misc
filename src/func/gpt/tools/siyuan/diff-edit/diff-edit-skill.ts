

const BLOCK_DIFF_EDIT_SKILL = `
# Block Diff 编辑规范（严格模式）

## ⚠️ 重要守则

### 0. 首先问自己：需要用 applyDiff 吗？

**编辑出错的责任完全在于你！**

| 场景 | 推荐方案 |
|------|----------|
| 仅涉及内容增加 | → appendContent / createNewDoc |
| 极端复杂变更（移动块、重构文档） | → 让用户自己来 |
| 跨文档编辑、大范围编辑 | → 让用户自己来 |
| 中等复杂、小范围更新 | → applyDiff ✅ |
| 复杂嵌套结构（如列表） | → append 到原块后让用户整理 |

### 1. 编辑出错需要回滚时

告知用户：
1. 右上角文档菜单可查看文件历史恢复版本
2. 插件顶栏"变量管理"可查看过去工具调用，可能检索到旧版本

### 2. 规划指南

- **一次最多处理 3 个 hunk**
- 批量编辑 → 拆解为多个小任务逐步完成
- 只能用于**同邻域范围**内编辑

### 3. 精细化编辑策略

| 目标类型 | 策略 |
|----------|------|
| 叶子块 | 直接编辑或使用 REPLACE 命令 |
| 容器块小编辑 | getBlockContent(showId=true, showSubStructure=true) -> 精细化编辑子块 |
| 容器块大改动（结构不变） | 替换整个容器 (建议使用 REPLACE 命令) |
| 大容器极大改动 | 让用户自己来 |

---

## 核心理念

Block Diff 是**严格模式**的编辑方式：
- 所有编辑在执行前会进行**强制内容匹配校验**
- 任何一个 hunk 校验失败，**整个 diff 都会被拒绝**
- 必须先用 \`getBlockContent(showId=true)\` 获取准确内容

---

## Diff 语法规范

### 基本格式

\`\`\`diff
@@<blockId>@@
  保留不变的上下文行（空格开头）
- 要删除的行（减号开头）
+ 要添加的行（加号开头）
\`\`\`

### 行类型说明

| 前缀 | 格式 | 含义 | 校验规则 |
|------|------|------|----------|
| \` \` | \`空格\` + 内容 | 上下文 C（保留不变） | 必须出现在原文中 |
| \`-\` | \`- \` + 内容 | 删除内容 A | 必须与原文匹配 |
| \`+\` | \`+ \` + 内容 | 新增内容 B | 无需校验 |

**CRITICAL - 格式要求**：
- 每行**必须且只能**有一个空格分隔前缀和内容
- ✅ 正确：\`- 要删除的内容\`
- ❌ 错误：\`-要删除的内容\`（缺少空格）
- ❌ 错误：\`-  要删除的内容\`（两个空格）

**空行处理**：
- 删除空行：写 \`-\`（只有减号）
- 添加空行：写 \`+\`（只有加号）
- 保留空行：写 \` \`（只有空格）或留空

**Markdown 列表特殊处理**：
如果内容本身以 \`-\` 开头（如列表项），需要两个减号：
\`\`\`diff
@@id@@
- - 旧的列表项
+ - 新的列表项
\`\`\`

### 校验规则（CRITICAL）

1. **上下文行 C** 必须出现在原文中
2. **删除行 A**（去掉 \`- \` 后）必须与原文匹配
3. **任何不匹配都会拒绝整个 diff**

### 操作类型推断

| diff 内容 | 操作类型 | 说明 |
|-----------|----------|------|
| 有 \`-\` 行和 \`+\` 行 | UPDATE | 更新块内容 |
| 只有 \`-\` 行 | DELETE | 删除该块 |
| 只有 \`+\` 行 | INSERT_AFTER | 在块后插入 |
| 只有上下文行 | ⚠️ 跳过 | 产生警告 |

### 特殊命令 （指令模式）

| 语法 | 作用 |
|------|------|
| \`@@DELETE:blockId@@\` | 删除整个块（无需内容） |
| \`@@REPLACE:blockId@@\` | 直接替换整个块（跳过校验，需要 + 行） |
| \`@@BEFORE:blockId@@\` | 在块前插入 |
| \`@@AFTER:blockId@@\` | 在块后插入 |
| \`@@PREPEND:docId@@\` | 在文档/容器开头插入 |
| \`@@APPEND:docId@@\` | 在文档/容器末尾追加 |

**REPLACE vs 普通 UPDATE**：
- 普通 UPDATE：需要写出原内容，强制校验匹配
- REPLACE：跳过校验，直接用新内容替换（适用于不关心原内容的场景）

#### REPLACE的注意事项

- **保持简单原子化**：\`REPLACE\` 应当尽量保持简单原子化。
- **禁止复杂操作**：特别不允许复杂操作，比如把单个段落替换成复合结构。
- **拆分建议**：此类操作应该首先使用 \`REPLACE\` 更新当前块，然后使用 \`AFTER\` 或 \`APPEND\` 等命令添加后续块。

**示例对比**：

❌ **错误做法**（把一个块，一次性替换成多种块的复合）：
\`\`\`diff
@@REPLACE:20260108164554-m5ar6vb@@
+ ## 标题
+
+ 这是一个段落内容
+
+ - 列表项 1
+ - 列表项 2
+
+ \`\`\`python
+ print("code")
+ \`\`\`
\`\`\`

✅ **正确做法**（拆分为多个原子操作）：
\`\`\`diff
@@REPLACE:20260108164554-m5ar6vb@@
+ ## 标题

@@AFTER:20260108164554-m5ar6vb@@
+ 这是一个段落内容
+
+ - 列表项 1
+ - 列表项 2
+
+ \`\`\`python
+ print("code")
+ \`\`\`
\`\`\`

---

## 工作流程

### 第一步：获取内容

\`\`\`
getBlockContent(blockId, showId=true)
\`\`\`

返回格式：
\`\`\`
@@20260108164554-m5ar6vb@@段落
Hello World
This is line 2.

@@20260108164544-w8lz0zj@@标题
## Heading
\`\`\`

### 第二步：构造 Diff

**CRITICAL - 必须包含完整的原文内容！**

假设要修改 "Hello World" 为 "你好世界"：

\`\`\`diff
@@20260108164554-m5ar6vb@@
- Hello World
- This is line 2.
+ 你好世界
+ 这是第二行。
\`\`\`

**或者使用上下文行：**

\`\`\`diff
@@20260108164554-m5ar6vb@@
- Hello World
+ 你好世界
  This is line 2.
\`\`\`

**⚠️ 以下写法会被拒绝：**

\`\`\`diff
@@20260108164554-m5ar6vb@@
- Hello
+ 你好
\`\`\`
原因：原文是 "Hello World"，不是 "Hello"，内容不匹配！

### 第三步：执行

\`\`\`
applyBlockDiff({ diff: "..." })
\`\`\`

---

## 操作示例

### 更新块内容

\`\`\`diff
@@20260108164554-m5ar6vb@@
- Hello World
+ 你好，世界
\`\`\`

### 带上下文的更新

\`\`\`diff
@@20260108164554-m5ar6vb@@
  第一行保持不变
- 要修改的第二行
+ 修改后的第二行
  第三行保持不变
\`\`\`

### 删除块（方式一：通过 - 行）

\`\`\`diff
@@20260108164554-m5ar6vb@@
- 这个块的完整内容
- 第二行
\`\`\`

### 删除块（方式二：DELETE 命令）

\`\`\`diff
@@DELETE:20260108164554-m5ar6vb@@
\`\`\`

### 直接替换（REPLACE 命令，跳过校验）

\`\`\`diff
@@REPLACE:20260108164554-m5ar6vb@@
+ 全新的内容，不需要知道原来是什么
+ 第二行
+ 第三行
\`\`\`

**适用场景**：
- 不关心原内容是什么
- 原内容太长，复制麻烦
- 明确知道要整体替换

### 在块后插入

\`\`\`diff
@@20260108164554-m5ar6vb@@
+ 在目标块后面插入的新段落
\`\`\`

### 在文档末尾追加

\`\`\`diff
@@APPEND:20260108164554-m5ar6vb@@
+ 追加到文档末尾的内容
\`\`\`

### 批量操作

\`\`\`diff
@@20260108164554-m5ar6vb@@
- Hello
+ 你好

@@20260108164544-w8lz0zj@@
- ## Old Title
+ ## New Title
\`\`\`

---

## 容器块编辑

### 修改整个容器

\`\`\`diff
@@20251030184332-kjly5ar@@
- > 原始引述内容
- > 第二行
+ > 新的引述内容
+ > 更新后的第二行
\`\`\`

### 修改容器内子块

先用 \`showSubStructure=true\` 获取子块：

\`\`\`
getBlockContent(blockId, showId=true, showSubStructure=true)
\`\`\`

然后单独修改子块：

\`\`\`diff
@@20260108164611-5n7h75k@@
- 引述块内容
+ 修改后的引述内容
\`\`\`

---

## 错误处理

### 常见错误

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| 内容不匹配 | diff 中的内容与实际不符 | 重新获取最新内容 |
| 块不存在 | ID 错误或块已删除 | 重新获取确认 ID |
| 无效的块 ID | ID 格式不符合规范 | 从 getBlockContent 复制 |
| 没有有效操作 | 只有上下文行 | 添加 - 或 + 行 |

### 校验失败示例

**场景**：块实际内容是 "Hello World"

**错误的 diff**：
\`\`\`diff
@@id@@
- Hello
+ 你好
\`\`\`

**错误信息**：
\`\`\`
❌ 内容校验失败，拒绝执行编辑

Hunk #1 [id]
   错误类型: CONTENT_MISMATCH
   块内容不匹配，拒绝执行编辑
   期望内容: "Hello"
   实际内容: "Hello World"
\`\`\`

**正确的 diff**：
\`\`\`diff
@@id@@
- Hello World
+ 你好世界
\`\`\`

---

## 注意事项

1. **强制校验**：所有 hunk 都会校验，任何失败都拒绝整个操作
2. **完整内容**：必须提供完整的原文（context + minus）
3. **ID 格式**：yyyyMMddHHmmss-xxxxxxx（从 getBlockContent 复制）
4. **多行内容**：每行都需要正确的前缀（空格/-/+）
5. **空行**：空行默认视为上下文行
6. **操作数量**：建议每次最多 3 个 hunk
7. **复杂场景**：让用户自己处理
`.trim();

// 导出供 skill-doc.ts 使用
export { BLOCK_DIFF_EDIT_SKILL };
