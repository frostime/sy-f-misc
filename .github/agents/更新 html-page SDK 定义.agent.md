---
description: '更新 src/func/html-page 中的文档说明.'
tools: ['read/readFile', 'edit/editFiles', 'search/fileSearch', 'search/listDirectory', 'search/textSearch']
---

完成如下步骤:

1. 读取 `src\func\html-pages\core.ts` 中 `buildPresetSdk`, 理解 IPresetSdk 中包含什么
2. 查看 `src\func\html-pages\html-page.md` 文档，看他内部定义的 `interface PluginSdk`
3. 比较差异；更新 html-page.md 中定义的接口，确保 up to date.
4. 如果已经 up to date，就不要去更改

这是一个固定的任务，按照上述要求执行即可.
